TRADEX — BUILD WITH AI STRATEGY GENERATOR

I want to add an AI-powered strategy creation feature to the EXISTING TradeX Strategy Builder.

IMPORTANT:

Do NOT redesign the current Strategy Builder.

Do NOT change the existing TradeX UI, colours, layout, navigation, Markets, Journal, Alerts, Backtesting, Strategy Library, or Trading Concept Library.

Do NOT modify the existing Backtesting engine.

Do NOT modify historical-data functionality.

This task is ONLY to add the "Build with AI" functionality to the existing Strategy Builder.

==================================================
1. ADD "BUILD WITH AI"
==================================================

Add a clear button near the top of the existing Strategy Builder:

✨ Build with AI

When clicked, open a simple modal/panel.

Title:

BUILD A STRATEGY WITH AI

Description:

"Describe the strategy you want to build and TradeX will turn it into a strategy you can review and edit."

Text area:

[ Describe your strategy... ]

Buttons:

[ Generate Strategy ]

[ Cancel ]

==================================================
2. NORMAL LANGUAGE INPUT
==================================================

The user should be able to describe a strategy naturally.

Example:

"Make me a reversal strategy for Gold using liquidity sweeps and Fair Value Gaps on the 15 minute timeframe."

Another example:

"Create a strategy for US Tech 100 that looks for reversals using liquidity and FVG confirmation."

The AI should interpret the request and map it to the existing TradeX Strategy Builder fields.

==================================================
3. STRUCTURED TRADEX PROMPTS
==================================================

Also allow the user to paste a structured strategy prompt.

Example:

TRADEX STRATEGY

Name: Gold Liquidity Reversal

Market: XAUUSD

Direction: Both

Timeframes:
15M
5M

ENTRY

1. Concept: Liquidity Sweep
   Direction: Long
   Timeframe: 15M
   Rule: Candle is bullish

CONFIRMATION

1. Concept: Fair Value Gap
   Direction: Long
   Timeframe: 5M

EXIT

1. Concept: Take Profit

RISK

Risk per trade: 1%
Risk/Reward: 2:1

The AI should recognise this format and populate the Builder correctly.

==================================================
4. POPULATE THE EXISTING BUILDER
==================================================

After generation, DO NOT save the strategy automatically.

Instead, populate the existing Strategy Builder.

Fill in the existing Builder fields for:

- Strategy name
- Description
- Market/instrument
- Direction
- Timeframes
- Entry conditions
- Confirmation conditions
- Exit conditions
- Risk management

The generated strategy must use the SAME data structures as strategies created manually.

Do NOT create a separate AI strategy format.

==================================================
5. USER MUST BE ABLE TO EDIT IT
==================================================

After the AI generates the strategy, the user must be able to use the existing Builder normally.

They should be able to:

- Edit the strategy name
- Change the market
- Change direction
- Change timeframe
- Edit conditions
- Add conditions
- Remove conditions
- Reorder conditions
- Change concepts
- Change rules
- Edit risk settings

Nothing should be locked because it was generated by AI.

==================================================
6. USE THE EXISTING CONCEPT LIBRARY
==================================================

The AI must use the existing TradeX Trading Concept Library.

Do NOT invent new concepts.

Use the existing concepts and aliases already implemented in TradeX.

For example, understand common terminology such as:

FVG
IFVG
BOS
CHoCH
MSS
SMT
HTF
LTF
OB
Order Block
Liquidity Sweep
Liquidity Grab
AMD
PO3
IRL
ERL
HH
HL
LH
LL

Map them to the actual concepts available in the TradeX library.

If the user's requested concept does not exist in TradeX, do not invent it.

Clearly tell the user that the concept is not currently available.

==================================================
7. USE EXISTING BUILDER RULES
==================================================

The AI must use the existing rules available in the Strategy Builder.

Do NOT invent technical rules.

Examples of existing rules may include:

- Candle is bullish
- Candle is bearish
- Price is above the candle open
- Price is below the candle open
- Price crosses above the previous high
- Price crosses below the previous low
- Always true

Only use rules that actually exist in the current TradeX Builder.

==================================================
8. CLEAR GENERATION RESULT
==================================================

After generation, show a simple confirmation before returning the user to the Builder:

STRATEGY GENERATED

Strategy:
Gold Liquidity Reversal

Market:
XAUUSD

Direction:
Both

Timeframes:
15M · 5M

Entry:
2 conditions

Confirmation:
1 condition

Exit:
1 condition

Risk:
1% · 2:1

Then provide:

[ Review Strategy ]

The existing Strategy Builder should then contain the generated strategy.

==================================================
9. NO AUTOMATIC SAVING
==================================================

The AI must NEVER automatically:

- Save the strategy
- Create a strategy version
- Activate the strategy
- Run a backtest
- Create alerts
- Create monitoring records

The user must explicitly decide what to do after reviewing the generated strategy.

==================================================
10. VALIDATION
==================================================

Before putting the generated strategy into the Builder, validate the generated data.

Make sure:

- Strategy name is valid
- Market is valid
- Direction is valid
- Timeframes are valid
- Concepts exist
- Rules exist
- Conditions have valid stages
- Required fields are populated
- Risk values are valid

If something cannot be understood or mapped, do not silently guess.

Clearly show what needs to be reviewed.

==================================================
11. DO NOT SILENTLY CHANGE THE USER'S REQUEST
==================================================

If the user asks:

"Make me a Gold reversal strategy using liquidity sweeps and FVG."

Do not silently replace those concepts with completely different concepts.

If something cannot be mapped to TradeX, tell the user.

==================================================
12. ERROR HANDLING
==================================================

If AI generation fails:

Show:

"Unable to generate strategy."

Provide:

[ Try Again ]

Do not create a partially corrupted strategy.

If only part of the request can be understood, clearly explain which part requires user input.

==================================================
13. PRESERVE MANUAL BUILDER
==================================================

The existing manual Strategy Builder must continue working exactly as it currently does.

Users should have two ways to start:

Create strategy manually

OR

✨ Build with AI

Both must ultimately use the same Strategy Builder.

==================================================
14. RESPONSIVE DESIGN
==================================================

Keep the existing responsive design.

Test the new AI panel at:

375px
402px
768px
1440px

Make sure:

- Text area is usable
- Buttons are accessible
- No horizontal scrolling occurs
- Generated strategy summary is readable
- Existing Builder remains usable

==================================================
15. TESTING
==================================================

Actually test the feature in the running application.

Test:

1. Open Build with AI.

2. Enter a normal-language strategy request.

3. Generate the strategy.

4. Confirm the existing Builder is populated.

5. Edit the generated strategy.

6. Add a condition.

7. Remove a condition.

8. Change a timeframe.

9. Change the strategy name.

10. Cancel the AI generation.

11. Try a structured TradeX strategy prompt.

12. Verify concepts are correctly mapped.

13. Test an unknown concept and confirm it is not invented.

14. Reload the application and confirm the existing manual Builder still works.

==================================================
16. REGRESSION
==================================================

Do not break existing TradeX functionality.

Run:

Frontend tests
API tests
Frontend typecheck
API typecheck
Production build
git diff --check

Check the browser console for errors.

==================================================
FINAL GOAL
==================================================

I want this simple workflow:

User clicks:

✨ Build with AI

↓

User types:

"Make me a reversal strategy for Gold using liquidity sweeps and FVG confirmation on the 15 minute timeframe."

↓

TradeX understands the request.

↓

TradeX finds the matching existing concepts and rules.

↓

TradeX generates the strategy.

↓

The EXISTING Strategy Builder is automatically populated.

↓

User reviews and edits it.

↓

User can then use the normal Builder workflow.

The AI is simply an easier way to CREATE the strategy.

It must not replace the existing Builder or create a separate strategy system.

IMPORTANT:

This task is ONLY for the Build with AI feature.

Do NOT make changes to Backtesting, historical data, Markets, Journal, Alerts, Monitoring, or other unrelated TradeX functionality.